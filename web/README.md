# Web Playground

The 4ward web playground: write P4, install entries, inject packets,
and step through the trace — all in the browser.
`bazel run //web:playground` opens `http://localhost:8080`. See
[`docs/ENTRY_POINTS.md`](../docs/ENTRY_POINTS.md#web-playground).
