# Distinct Bazel workspace — does NOT inherit the root `.bazelrc`.
# Minimum flags for `bazel build @fourward//...` from here, shared
# by local invocations and the `bcr-consumer-check` CI job. CI only
# adds BuildBuddy remote-cache flags on top.

common --enable_bzlmod
common --noenable_workspace

build --cxxopt=-std=c++20
build --host_cxxopt=-std=c++20

# Hermetic JDK so local builds don't depend on whatever system Java
# happens to be installed (same rationale as the root `.bazelrc`).
build --java_runtime_version=remotejdk_21
build --tool_java_runtime_version=remotejdk_21
