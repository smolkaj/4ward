# Simulator

The Kotlin simulator — the heart of 4ward and the source of truth for
all dataplane state. [`ir.proto`](ir.proto) is the behavioral IR, the
core contract with the p4c backend. See [`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md).
