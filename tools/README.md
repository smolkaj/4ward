# tools

Developer scripts. Run `./tools/dev.sh help` for a listing.
Run `./tools/format.sh && ./tools/lint.sh` before every commit — CI runs
the same scripts, so passing locally means passing in CI.
