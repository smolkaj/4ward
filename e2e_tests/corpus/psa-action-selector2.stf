# PSA ActionSelector: CRC32 hash, two selector fields.
# Tests: ActionSelector with multiple selector key fields.
# No table entry (selector requires action profile member/group programming).
# No send_to_port → packets dropped by PSA default.

packet 0 001122334455 00aabbccddee 0800
