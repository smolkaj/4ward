# PSA Hash: 1-arg get_hash with multi-field+header list.
# Tests: Hash.get_hash({srcAddr, etherType, hdr.ipv4}) — mixed fields and headers.
# No send_to_port → packets dropped by PSA default.

add tbl hdr.ethernet.srcAddr:0x00aabbccddee a1()

packet 0 001122334455 00aabbccddee 0800
