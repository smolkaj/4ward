# PSA ActionSelector: selector keys but no psa_implementation property.
# Tests: Table with selector match kind but no ActionSelector binding.
# No send_to_port → packets dropped by PSA default.

packet 0 001122334455 00aabbccddee 0800
