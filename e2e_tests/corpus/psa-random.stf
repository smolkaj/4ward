# PSA Random: random value generation stored in metadata.
# Tests: Random extern instantiation and read().
# No send_to_port → packets dropped by PSA default.

add tbl hdr.ethernet.srcAddr:0x00aabbccddee execute_random()

# Table hit → execute_random generates random value in [200, 400]
packet 0 001122334455 00aabbccddee 0800
