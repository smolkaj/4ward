# PSA optional match: table with optional + exact match kinds.
# Tests: Optional match kind (PSA-specific), parser mask/range select.
# No send_to_port → packets dropped by PSA default.

# IPv4 packet with etherType in mask range (0x0800 &&& 0x0F00)
packet 0 001122334455 00aabbccddee 0800 45000028 00000000 4006AE26 C0A80101 0A000101
