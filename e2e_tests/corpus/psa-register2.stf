# PSA Register: write metadata to register at action parameter index.
# Tests: Register.write() extern method.
# No send_to_port → packets dropped by PSA default.

add tbl hdr.ethernet.srcAddr:0x00aabbccddee execute_register(idx:0)

packet 0 001122334455 00aabbccddee 0800
