# PSA Counter: direct counter.count() call in apply block (no table).
# Tests: Counter extern called outside of a table action.
# No send_to_port → packets dropped by PSA default.

packet 0 001122334455 00aabbccddee 0800
