ignore_directories([
    # Bazel output symlinks. The last component depends on the workspace
    # directory name (bazel-4ward, bazel-bcr_test_module, …) so use a glob
    # rather than listing each one.
    "bazel-*",
    "**/bazel-*",
    # Bazel sub-projects brought in by p4c's downstream targets.
    "control-plane/p4runtime",
    "test/frameworks/gtest",
    "bazel/example",
    # Misc build / cache directories.
    "build",
    "external",
    ".claude",
])
